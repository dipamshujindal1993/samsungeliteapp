import { StyleSheet } from 'react-native'
import {
    Colors
} from '@resources'

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.white
    },
    webview: {
        flex: 1,
        marginHorizontal: 25
    },
    spinner: {
        backgroundColor: Colors.white,
    },
})